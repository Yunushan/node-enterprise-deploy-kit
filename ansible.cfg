[defaults]
inventory = ansible/inventory.example.yml
roles_path = ansible/roles
# Keep SSH host key verification enabled by default. Use an inventory-specific
# override for throwaway labs if your organization permits it.
host_key_checking = True
retry_files_enabled = False
stdout_callback = default
